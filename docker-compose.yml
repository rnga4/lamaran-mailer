services:
  mailer:
    build: .
    env_file: .env
    environment:
      # "Hari" (limit harian Gmail & reset) mengikuti waktu lokal WIB
      - TZ=Asia/Jakarta
    ports:
      - "8086:8000"
    volumes:
      - ./cv:/app/cv
      - ./data:/app/data
